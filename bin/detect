#!/bin/sh

# This pack requires mecab and mecab-ipadic

if [ -f /app/.apt/usr/lib/mecab ]; then
  echo "mecab-ipadic-NEologd"
  exit 0
else
  # TODO fix
  echo "detect failed but we'll understand why later"
  exit 0
fi